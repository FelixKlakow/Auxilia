# Auxilia.Ai.Tests

Tests for `Auxilia.AI`. `LocalOllamaTests` requires a running Ollama instance and is not tagged `Unit` — it does not run in CI.

## File / Folder Map
```
Auxilia.Ai.Tests/
├── UnitTest1.cs                              # Placeholder — expand with builder immutability, event ordering, tool filter tests
└── LocalOllamaTests/
    └── ExampleConsumerOllamaTests.cs         # End-to-end session test against local Ollama (not CI)
```